export const MENU_PLATE_CSS = `
      box-sizing: border-box;
      padding: 4px;
      background: var(--scribe-surface, #ffffff);
      border: 1px solid var(--scribe-line, #e4e8ef);
      border-radius: 8px;
      box-shadow: var(--scribe-menu-shadow, 0 1px 2px rgba(20, 30, 60, .10), 0 5px 14px rgba(20, 30, 60, .12));
      font-size: 13px;
      line-height: 1.4;
      color: var(--scribe-ink, #1f2530);
      user-select: none;`;

export const MENU_ROW_CSS = `
      padding: 4px 10px;
      border-radius: 4px;
      white-space: nowrap;`;

export const MENU_ROW_NOICON_CSS = `
      padding: 4px 12px;
      border-radius: 4px;
      white-space: nowrap;`;

export const MENU_SEP_CSS = `
      height: 1px;
      border: none;
      background: var(--scribe-line, #e4e8ef);
      margin: 4px -4px;`;
